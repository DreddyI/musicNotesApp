import React, {Component} from 'react';
import { View, Text } from 'react-native';

class Results extends Component {
    render() {
      return (
        <View style={{flex:1}}>
          <Text>тут будут результаты</Text>
        </View>
      );
    }
}

export default Results;